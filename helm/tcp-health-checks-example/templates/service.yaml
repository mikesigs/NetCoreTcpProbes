apiVersion: v1
kind: Service
metadata:
  name: {{ include "tcp-health-checks-example.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "tcp-health-checks-example.name" . }}
    helm.sh/chart: {{ include "tcp-health-checks-example.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "tcp-health-checks-example.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
